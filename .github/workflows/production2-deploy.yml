name: production2 CI test

on:
  push:
    branches:
      - production2

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow trigger
        run: |
          echo "Workflow triggered on production2"
          echo "Commit: $GITHUB_SHA"
          echo "Branch: $GITHUB_REF"

